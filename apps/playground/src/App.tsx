import { ReactPackageSmoke } from "./ReactPackageSmoke.js";

function App() {
  return (
    <main className="min-h-screen bg-canvas px-[clamp(18px,3vw,40px)] py-[clamp(18px,3vw,40px)] max-md:px-3.5 max-md:py-[18px]">
      <ReactPackageSmoke />
    </main>
  );
}

export { App };
