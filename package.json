{
  "name": "sopflow-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Sopflow monorepo",
  "type": "module",
  "packageManager": "pnpm@11.21.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/howlil/sopflow.git"
  },
  "scripts": {
    "lint": "biome check .",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm --filter playground typecheck",
    "pack:core": "pnpm --filter @sopflow/core pack"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.13"
  },
  "license": "MIT"
}
